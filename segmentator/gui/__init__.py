"""The PyQt6 config editor: author a pipeline YAML with a live preview of it.

Optional. ``pip install segmentator[gui]`` — nothing in :mod:`segmentator` itself
imports this package, so a headless batch install never pulls Qt in.

    segmentator-gui configs/motion.yaml
"""
