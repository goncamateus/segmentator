"""segmentator — a configuration-driven image and video segmentation engine.

Part of the goncanalyser suite: goncanalyser is the interactive Qt workspace for
tuning an operator chain, segmentator is the headless engine that runs the tuned
recipe over a batch from a YAML config.
"""
