\section{Limitations and maturity}
% INTENT: state plainly, as a list. No labelled data, therefore no accuracy,
% IoU or false-alarm-rate claim anywhere in this document. Validated on one
% camera and one scene type. Single-threaded CPU only; the GPU in the bench
% machine is idle. The fixed mean-of-N background assumes a static camera. No
% online/streaming deployment, no alerting, no calibration to concentration.
% Maturity: validated on real footage, not deployed --- TRL 4.
